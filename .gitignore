# PlatformIO build output (fully reproducible from source + platformio.ini)
.pio/
.pioenvs/
.piolibdeps/

# Editor / OS cruft
.vscode/
*.swp
.DS_Store

# Firmware backups (see tools/backup_firmware.sh) -- binaries, not source,
# so they don't belong in git history, but the dir is kept locally.
firmware_backups/

# macOS harness: compiled binary and generated frames (regenerate with
# ./host/shots.sh). Curated stills live in docs/ instead.
host/skokan
host/skokan.dSYM/
host/shots/
